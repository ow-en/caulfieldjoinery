# Copy this file to .env and fill in real values.
# .env should be in .gitignore — never commit real credentials.

DATABASE_URL=postgres://postgres:password@localhost:5432/caulfield
RUST_LOG=caulfield_api=debug,tower_http=debug
PORT=8080
